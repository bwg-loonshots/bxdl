# 저장소 작업 규칙

- BXDL 커밋과 PR에는 BXDL 저장소의 변경만 포함한다. NIGO 변경은 별도로 검토·공유한다.
- NIGO 저장소를 수정해야 하면 작업 전에 전용 Git worktree와 작업 브랜치를 만든다. 공유 중인 NIGO 기본 checkout에서 직접 수정하거나 기존 미커밋 변경을 가져오지 않는다.
- PR 제목과 본문은 한글로 작성한다. 실제 구현 범위, 검증 결과와 아직 제공하지 않는 기능을 구분한다.
- 코드 검증은 `make check`를 기준으로 한다. 생성된 실행 파일, archive, 키, 고객 데이터와 임시 작업물은 커밋하지 않는다.
